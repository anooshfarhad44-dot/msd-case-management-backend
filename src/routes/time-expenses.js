const express = require("express");
const router = express.Router();
// We'll define specific sub-routes if needed, but time-expenses are tied to matters.
// So we can export this and let matters.js use it, or mount it standalone.

module.exports = router;
